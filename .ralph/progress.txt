
## TASK-001: Create src/lib/db.ts with SQLite schema
- Date: 2026-04-18
- Status: done
- Created src/lib/db.ts with better-sqlite3, auto-creates data/ directory, WAL mode.
- Tables: users, tournaments (with lock_time_groups + lock_time_knockout), predictions (group_predictions, third_place_picks, knockout_picks, tiebreaker), groups (scoring_settings, max_brackets, submissions_locked), group_members (group_id + prediction_id PK), messages.
- Followed March Madness pattern: singleton db instance, lazy init, pragma WAL.
- Lessons: Keep schema minimal — no migrations needed yet since this is a fresh project. The group_members table uses prediction_id (not user_id) as PK pair, matching the task spec for bracket-level group membership.

## TASK-002: Create src/lib/auth.ts with JWT, hashing, getAuthUser
- Date: 2026-04-18
- Status: done
- Created src/lib/auth.ts: hashPassword, verifyPassword (bcryptjs), signToken, verifyToken (jsonwebtoken), getAuthUser(request).
- Added AuthUser interface to src/types/index.ts with {userId, username, isAdmin}.
- getAuthUser reads 'token' cookie from request headers using the 'cookie' package parse().
- Followed March Madness pattern but adapted: uses Request object instead of Next.js cookies() for API route compatibility.
- Helper functions setTokenCookie/clearTokenCookie for Set-Cookie header strings.
- Lessons: Using named constants (SALT_ROUNDS, TOKEN_EXPIRY, MAX_AGE_SECONDS, TOKEN_NAME) instead of magic values. The 'cookie' package was already in dependencies.

## TASK-003: Create auth API - POST register/login (2026-04-18)
- Created src/app/api/auth/route.ts with POST handler for register and login actions.
- Register: validates username/password length, checks uniqueness, hashes password, inserts user, returns JWT in httpOnly cookie.
- Login: finds user by username, verifies password, returns JWT in httpOnly cookie.
- Used proper UserRow interface instead of `any` for the DB query result.
- Used named constants for validation limits (MIN_PASSWORD_LENGTH, MAX_USERNAME_LENGTH, MAX_PASSWORD_LENGTH).
- Leveraged existing auth.ts helpers: signToken, setTokenCookie, hashPassword, verifyPassword.
- Build and tsc passed first try. Deploy verified on attempt 3 (waited for webhook rebuild).

## TASK-004: Add GET session and DELETE logout handlers to auth API
- Date: 2026-04-18
- Added GET handler to /api/auth that reads JWT via getAuthUser and returns {ok, user} or {ok:false, user:null}
- Added DELETE handler to /api/auth that clears the token cookie with max-age=0
- Both getAuthUser and clearTokenCookie already existed in auth.ts — just needed to import and use them
- Lesson: Check existing utilities before writing new code — auth.ts already had everything needed

## TASK-005: Create useAuth hook and AuthProvider (2026-04-18)
- Created src/hooks/useAuth.tsx with AuthProvider context and useAuth() hook.
- Follows March Madness pattern: context with user/loading state, login/register/logout callbacks.
- Inlined fetch calls instead of separate api helper — keeps it minimal for now.
- Build and tsc pass. Deploy verified on server.
- Lesson: No api.ts abstraction needed yet — can extract later if fetch patterns repeat across many hooks.

## TASK-006: Create ThemeRegistry, useThemeMode, globals.css
- Date: 2026-04-18
- Status: done
- Files: src/hooks/useThemeMode.tsx, src/components/common/ThemeRegistry.tsx, src/app/globals.css
- Lessons:
  - Followed March Madness ThemeRegistry pattern exactly — context provider wrapping ThemeProvider+CssBaseline.
  - Soccer palette: primary #2e7d32 (green), secondary #ffc107 (gold). Named constants, no inline hex.
  - globals.css kept minimal: box-sizing reset, body height, font smoothing, link reset.
  - No Emotion cache setup needed — MUI 7 handles it automatically with ThemeProvider.

## TASK-007: Wire providers into layout.tsx (2026-04-18)
- Created `src/components/common/Providers.tsx` as a 'use client' wrapper combining ThemeRegistry + AuthProvider.
- Updated `src/app/layout.tsx` to import globals.css and wrap children in Providers.
- Pattern: Server component layout delegates to a single client wrapper for all providers. This keeps layout.tsx as a server component (needed for metadata export) while enabling client-side context.
- Build passed first try. Deploy verified after ~40s.

## TASK-008: Create AuthForm component (2026-04-18)
- Created src/components/auth/AuthForm.tsx with MUI Card, Tabs for login/register toggle, TextField inputs, Alert for errors, CircularProgress loading state.
- Followed March Madness AuthForm pattern but used Card instead of Paper, Alert instead of Typography for errors, and added disabled states during submission.
- Used `slotProps.htmlInput` instead of deprecated `inputProps` for MUI v7 TextField.
- Used proper error typing (`e instanceof Error`) instead of `any` per anti-patterns.
- Nova Act test: `ActResult` has no `.response` or `.result` attribute — only `.metadata`. For verification, just call `act()` and check it doesn't throw.
- Component is not rendered on any page yet (that's TASK-012), so browser test just verified site still loads.

## TASK-009: Create Navbar and add to layout (2026-04-18)
- Created src/components/common/Navbar.tsx following March Madness pattern
- MUI AppBar with ⚽ World Cup Predictions title, nav links (Predictions, Leaderboard, Groups), Admin link for admins, theme toggle, auth status (username + logout or login)
- Mobile responsive: hamburger menu with Drawer for small screens
- Added Navbar to Providers.tsx (renders above children inside AuthProvider)
- Nav links only shown when user is logged in
- Nova Act verified: Navbar visible with correct text and theme toggle on production
- Lesson: Nova Act ActResult has no response/matches_intent attribute — just use it for visual verification via logs, don't assert on result attributes

## TASK-010: Create CountdownTimer component (2026-04-18)
- Created src/components/common/CountdownTimer.tsx
- Props: targetDate (ISO string), optional label
- Shows days/hours/min/sec with 1-second interval updates
- Displays "Locked" in error color when target date has passed
- Uses useEffect with cleanup (clearInterval) on unmount
- Syncs to targetDate prop changes via useEffect dependency
- Uses MUI theme tokens only (primary.main, text.secondary, error.main)
- Lessons: Keep component simple — extracted calcTimeLeft as pure function, used UNIT_LABELS constant array to avoid inline strings. padStart(2, "0") for consistent display.

## TASK-011: Create tournament GET API (2026-04-18)
- Created src/app/api/tournaments/route.ts with GET handler
- Returns most recent tournament by year DESC, parsing bracket_data and results_data from JSON strings
- Returns {ok: true, tournament: null} when no tournament exists
- Build and tsc pass clean. Deployed and verified 200.
- Lesson: Simple API routes are straightforward — just parse the JSON string columns from SQLite before returning.

## TASK-012: Create home page with countdown and auth (2026-04-18)
- Implemented home page as 'use client' component fetching tournament from /api/tournaments
- Shows tournament name, year, CountdownTimer to lock_time_groups when tournament exists
- Shows AuthForm when not logged in, welcome message + "Make Your Predictions" link when logged in
- Shows "No tournament" message when no tournament exists
- Loading spinner while fetching auth state and tournament data
- Nova Act verified: page loads correctly showing heading and no-tournament message
- Lesson: Nova Act's ActResult only has `metadata` attribute — success is determined by no exception being thrown, not by checking a response attribute

## TASK-013: Create bracketData utilities and seed data (2026-04-18)
- Created src/lib/bracketData.ts with utility functions and 2026 FIFA World Cup seed data
- Utilities: parseBracketData, getTeamByName, getGroupByName, getTeamSeed, getTeamRanking
- 48 teams across 12 groups (A–L), 4 teams per group with real team names, FIFA rankings, pot assignments
- Helper functions (team, group) keep the data declaration concise and type-safe
- Build and tsc pass clean. Deployed and verified 200.
- Lesson: Keep seed data in a single exported constant (WORLD_CUP_2026_DATA) — easy to import from admin page or tests. Helper factory functions reduce repetition without adding complexity.

## TASK-014: Add result types to index.ts
- Date: 2026-04-18
- Status: done
- Added GroupStageResults, KnockoutResults (Record<string,string>), and TournamentResults interfaces to src/types/index.ts
- Placed them before GroupPrediction since TournamentResults references KnockoutMatchup (already defined below)
- Both tsc --noEmit and next build passed first try
- Lessons: Pure type additions are low-risk; no runtime impact, just need to verify build passes

## TASK-015: Create admin API - POST create tournament (2026-04-18)
- Created src/app/api/admin/route.ts with POST handler
- Verifies admin via getAuthUser, returns 403 if not admin
- Accepts {name, year, lock_time_groups, lock_time_knockout, bracket_data}, inserts with randomUUID
- Handles bracket_data as either string or object (JSON.stringify if object)
- Validates name and year are required, returns 400 if missing
- Build and tsc passed first try. Deploy verified on attempt 4.
- Lesson: Keep API routes minimal — just auth check, validation, DB insert, response. No need for extra abstractions at this stage.

## TASK-016: Create admin page - tournament creation (2026-04-18)
- Created src/app/admin/page.tsx with admin-only access guard
- Shows existing tournament details if one exists
- Create form with name, year, lock times (datetime-local inputs)
- "Use 2026 World Cup Data" button loads WORLD_CUP_2026_DATA into bracket_data
- Submits to POST /api/admin
- Lessons: NovaAct uses `starting_page` not `starting_url`, and `metadata` not `response` on ActResult

## TASK-017: Add seed test tournament button to admin page (2026-04-18)
- Added "Seed Test Tournament" button to admin page, only visible when no tournament exists
- One-click creates tournament with name "FIFA World Cup", year 2026, lock_time_groups 2026-06-11, lock_time_knockout 2026-06-30, using WORLD_CUP_2026_DATA
- Used named constants for seed values (SEED_TOURNAMENT_NAME, SEED_TOURNAMENT_YEAR, SEED_LOCK_GROUPS, SEED_LOCK_KNOCKOUT)
- Placed in a "Quick Setup" card above the manual create form
- Build and tsc passed first try. Deploy verified on attempt 3.
- Lessons: Nova Act ActResult uses str() for content inspection, not .response attribute. Keep seed button minimal — reuses existing POST /api/admin endpoint.

## TASK-018: Create group stage scoring function (2026-04-18)
- Created src/lib/scoring.ts with scoreGroupStage function
- Per-team scoring: advance_correct (boolean match), exact_position, upset_bonus (prediction-gated, capped at predicted position)
- Per-group bonuses: advancement_correct_bonus (all 4 advance/not-advance correct), perfect_order_bonus (all 4 exact positions)
- Advance logic: predicted 1st/2nd = advance, predicted 3rd + in thirdPlacePicks = advance, predicted 4th = not advance
- Upset bonus: max(0, seed - predictedPosition) * upsetBonusPerPlace, only when actualPosition <= predictedPosition
- Returns GroupStageScoreResult with total and perGroup detail array
- Exported GroupScoreDetail and GroupStageScoreResult interfaces for use by leaderboard/breakdown components
- Build and tsc passed first try. Deploy verified on attempt 4.
- Lessons: Keep scoring as pure functions with no DB dependency — easy to test and reuse. The independence of advancement_correct_bonus and perfect_order_bonus is key — perfect order does NOT imply advancement correct due to 3rd-place pick interaction.

## TASK-019: Create knockout scoring function (2026-04-18)
- Added `scoreKnockout()` and `scoreTotalPrediction()` to `src/lib/scoring.ts`
- Knockout scoring: base points per round, upset bonus using FIFA rankings with modulus formula, champion bonus for correct Final pick
- `scoreTotalPrediction()` combines group stage + knockout into a single result
- Lessons: Keep scoring functions pure — they take data in and return structured results. The `KnockoutMatchup.round` field (0-5) maps directly to the `KNOCKOUT_ROUNDS` array indices, making round lookup straightforward.

## TASK-020: Add scoring unit tests with vitest (2026-04-18)
- Added vitest 3.1.2 as devDependency, created vitest.config.ts with @/ alias
- Created tests/scoring.test.ts with 28 tests covering:
  - Group stage: advance_correct, exact_position, upset bonus (capped at prediction), 3rd-place interaction, independent advancement/perfect order bonuses, multi-group sums, missing predictions, custom settings
  - Knockout: base points per round, upset bonus with modulus and round multipliers, champion bonus, unplayed matches, 3rd place match, custom settings
  - Integration: scoreTotalPrediction combining both, handling missing results
- All tests pass, build clean
- Lesson: vitest resolves @/ alias via vitest.config.ts resolve.alias, not tsconfig paths

## TASK-052 — Fix home page: show AuthForm when not logged in regardless of tournament state
- **Date**: 2026-04-18
- **Status**: Done
- **Changes**: Restructured src/app/page.tsx to check auth first. Unauthenticated users always see title + subtitle + AuthForm. Authenticated users see tournament info (if exists) + welcome + navigation. Added Admin Panel button for admin users.
- **Lessons**: Nova Act `act()` returns `ActResult` with only `metadata` field — no `success` or `response` attribute. A successful act just completes without raising. Use `nova.act()` without asserting on return value. March Madness pattern of `if (!user) return <AuthForm />` is clean and should be the default for all projects.

## TASK-021: Create predictions API - save/load group predictions
- Date: 2026-04-18
- Created src/app/api/picks/route.ts with GET and POST handlers
- GET: returns user's prediction for the active tournament (parses JSON fields)
- POST action='save_groups': upserts prediction with bracket_name, group_predictions, third_place_picks
- Enforces lock_time_groups check, rejects saves after lock
- Follows existing patterns from auth and tournaments routes (getAuthUser, getActiveTournament helper)
- Build passed first try, deploy verified in ~30s
- Lessons: Kept the route under 150 lines by extracting saveGroups as a separate function. Reused the tournament fetch pattern from tournaments/route.ts.

## TASK-053: Fix WORLD_CUP_2026_DATA with real draw results and FIFA rankings
- Date: 2026-04-18
- Replaced all 12 groups with actual 2026 FIFA World Cup draw results
- Updated all 48 team names, FIFA rankings, and pot assignments
- No other files referenced old team names — clean replacement
- All 28 scoring tests still pass (they use inline test data, not WORLD_CUP_2026_DATA)
- Lessons: The old data was placeholder; always verify seed data against official sources before shipping

## TASK-022: Add save/load knockout picks to predictions API (2026-04-18)
- Added `save_knockout` action to POST /api/picks
- Checks lock_time_knockout before allowing saves
- Validates group stage results exist before allowing knockout picks
- Requires existing group predictions (can't submit knockout without groups first)
- Tiebreaker is sanitized (rounded, non-negative)
- GET already returned knockout_picks and tiebreaker — no changes needed
- Lessons: The existing route was well-structured, just needed the new action handler appended. Kept the same pattern as saveGroups for consistency.

## TASK-023: Create GroupPrediction component (2026-04-18)
- Created src/components/bracket/GroupPrediction.tsx with reorderable team list
- Uses MUI Card, IconButtons for up/down arrows, Chip for pot seed, FIFA ranking display
- Props: groupName, teams, order, onChange, disabled
- Component is ~90 lines, well under the 150-line limit
- No naming conflict with GroupPrediction type in types/index.ts since component is default export
- Lesson: The bracket/ directory didn't exist yet — needed to create it first
